# kube-prometheus-stack values for geekzoo-prod

defaultRules:
  create: true

alertmanager:
  enabled: true
  config:
    route:
      group_by: ["alertname"]
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 4h
      receiver: default
    receivers:
      - name: default
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: ceph-block
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi

grafana:
  enabled: true
  # Recreate is required with a single RWO PVC: RollingUpdate surges a second
  # pod against the same sqlite DB, which deadlocks the rollout and crashes
  # startup provisioning with "database is locked" races.
  deploymentStrategy:
    type: Recreate
  ingress:
    enabled: false
  admin:
    existingSecret: grafana-admin-secret
    userKey: admin-user
    passwordKey: admin-password
  envValueFrom:
    GF_AUTH_GENERIC_OAUTH_CLIENT_ID:
      secretKeyRef:
        name: grafana-oidc-secret
        key: GF_AUTH_GENERIC_OAUTH_CLIENT_ID
    GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET:
      secretKeyRef:
        name: grafana-oidc-secret
        key: GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET
  persistence:
    enabled: true
    storageClassName: ceph-block
    size: 10Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi
  additionalDataSources:
    - name: Loki
      type: loki
      uid: loki
      url: http://loki.observability.svc:3100
      access: proxy
      isDefault: false
      jsonData:
        maxLines: 1000
    - name: Tempo
      type: tempo
      url: http://tempo.observability.svc:3200
      access: proxy
      isDefault: false
      jsonData:
        tracesToLogs:
          datasourceUid: loki
          filterByTraceID: true
          tags:
            - key: service.name
              value: k8s.pod.name
  grafana.ini:
    server:
      root_url: https://grafana.macbytes.io/
      serve_from_sub_path: false
    auth.generic_oauth:
      enabled: true
      name: Authentik
      allow_sign_up: true
      auto_login: false
      client_id: ${GF_AUTH_GENERIC_OAUTH_CLIENT_ID}
      client_secret: ${GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET}
      scopes: openid profile email
      auth_url: https://authentik.macbytes.io/application/o/authorize/
      token_url: https://authentik.macbytes.io/application/o/token/
      api_url: https://authentik.macbytes.io/application/o/userinfo/
      login_attribute_path: preferred_username
      name_attribute_path: name
      email_attribute_path: email
      role_attribute_path: contains(groups, 'Grafana Admins') && 'Admin' || contains(groups, 'Grafana Editors') && 'Editor' || 'Viewer'
      sign_out_redirect_url: https://authentik.macbytes.io/application/o/grafana/end-session/
      tls_skip_verify_insecure: false
    news:
      enable_feed: false
    dashboards:
      default_home_dashboard_path: /tmp/dashboards/k8s-resources-cluster.json

kubeApiServer:
  enabled: true

kubelet:
  enabled: true

kubeControllerManager:
  enabled: false

kubeEtcd:
  enabled: false

kubeScheduler:
  enabled: false

nodeExporter:
  enabled: true

prometheus-node-exporter:
  hostRootFsMount:
    enabled: true

prometheus:
  enabled: true
  prometheusSpec:
    retention: 15d
    # Accept remote_write from Tempo metrics-generator (service graphs / span metrics)
    enableRemoteWriteReceiver: true
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: ceph-block
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi
    # Running ~1.4Gi with Beyla network metrics; span-metrics from Tempo
    # generator adds more series — 2Gi limit left no headroom
    resources:
      requests:
        cpu: 200m
        memory: 1Gi
      limits:
        cpu: "1"
        memory: 3Gi

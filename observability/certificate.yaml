apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openobserve-certificate
  namespace: openobserve
spec:
  secretName: openobserve-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - openobserve.macbytes.io

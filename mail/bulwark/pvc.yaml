apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bulwark-data-pvc
  namespace: mail
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

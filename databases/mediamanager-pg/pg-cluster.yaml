apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: mediamanager-pg-cluster
  namespace: database
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:17.6-system-trixie
  instances: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
  enableSuperuserAccess: true
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: wasabi-object-store
        serverName: mediamanager-pg-cluster-geekzoo-prod
  bootstrap:
    initdb:
      database: MediaManager
      owner: MediaManager
      secret:
        name: pg-app-user-mediamanager
  storage:
    storageClass: ceph-block
    size: 10Gi

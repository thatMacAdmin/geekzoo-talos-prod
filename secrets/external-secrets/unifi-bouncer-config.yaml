apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: unifi-bouncer-config
  namespace: apps
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: unifi-bouncer-config
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/unifi-bouncer-config

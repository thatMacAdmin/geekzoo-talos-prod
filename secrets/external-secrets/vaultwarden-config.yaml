apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vaultwarden-config
  namespace: apps
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: vaultwarden-config
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/vaultwarden-config

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pg-app-user-blocky
  namespace: database
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: pg-app-user-blocky
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/pg-app-user-blocky

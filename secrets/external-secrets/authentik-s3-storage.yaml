apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-s3-storage
  namespace: identity
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: authentik-s3-storage
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/authentik-s3-credentials

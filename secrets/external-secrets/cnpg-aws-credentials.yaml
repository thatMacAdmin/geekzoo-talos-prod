apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cnpg-aws-credentials
  namespace: database
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: cnpg-aws-credentials
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/cnpg-aws-credentials

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-database-config
  namespace: identity
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: authentik-database-config
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/authentik-database-config

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bifrost-admin-credentials
  namespace: apps
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: secretstore-aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: bifrost-admin-credentials
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /k3s-geekzoo/bifrost/admin-credentials

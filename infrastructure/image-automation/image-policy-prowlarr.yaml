apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: prowlarr-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: prowlarr
  filterTags:
    pattern: '^release-[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$'
  policy:
    alphabetical:
      order: desc

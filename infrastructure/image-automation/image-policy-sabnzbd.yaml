apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: sabnzbd-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sabnzbd
  filterTags:
    pattern: '^([0-9]+\.[0-9]+\.[0-9]+(-ls[0-9]+)?)$'
  policy:
    semver:
      range: ">=4.0.0"

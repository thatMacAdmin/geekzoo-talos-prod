apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: blocky-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: blocky
  filterTags:
    pattern: '^v(?P<version>[0-9]+\.[0-9]+\.[0-9]+)$'
    extract: '$version'
  policy:
    semver:
      range: ">=0.29.0"

apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: audiobookshelf-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: audiobookshelf
  policy:
    semver:
      range: ">=2.0.0"

apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: bulwark-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: bulwark
  policy:
    semver:
      range: ">=1.7.0 <2.0.0"

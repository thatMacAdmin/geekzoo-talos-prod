apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flux-operator-certificate
  namespace: flux-system
spec:
  secretName: flux-operator-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - flux.macbytes.io

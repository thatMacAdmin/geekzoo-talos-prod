apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hubble-ui-ingressroute
  namespace: kube-system
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`hubble.macbytes.io`)
      kind: Rule
      services:
        - name: hubble-ui
          port: 80
  tls:
    secretName: hubble-ui-tls-certificate

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hubble-ui-certificate
  namespace: kube-system
spec:
  secretName: hubble-ui-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - hubble.macbytes.io

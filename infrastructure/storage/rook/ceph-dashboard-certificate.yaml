apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ceph-dashboard-certificate
  namespace: rook-ceph
spec:
  secretName: ceph-dashboard-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ceph.macbytes.io

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: seerr-tls-certificate
  namespace: media
spec:
  secretName: seerr-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - requests.macbytes.io

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bazarr-tls-certificate
  namespace: media
spec:
  secretName: bazarr-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - subtitles.macbytes.io

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sabnzbd-tls-certificate
  namespace: media
spec:
  secretName: sabnzbd-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - downloader.macbytes.io

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonarr-tls-certificate
  namespace: media
spec:
  secretName: sonarr-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - tv.macbytes.io

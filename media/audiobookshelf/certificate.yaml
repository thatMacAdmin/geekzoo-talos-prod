apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: audiobookshelf-tls-certificate
  namespace: media
spec:
  secretName: audiobookshelf-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - audiobooks.macbytes.io

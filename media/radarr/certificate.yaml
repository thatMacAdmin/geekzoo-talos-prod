apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-tls-certificate
  namespace: media
spec:
  secretName: radarr-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - movies.macbytes.io

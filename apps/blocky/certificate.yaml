apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: blocky-certificate
  namespace: apps
spec:
  secretName: blocky-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - blocky.macbytes.io

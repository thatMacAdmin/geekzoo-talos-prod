apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: edtheadmin-certificate
  namespace: apps
spec:
  secretName: edtheadmin-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - edtheadmin.io
    - www.edtheadmin.io

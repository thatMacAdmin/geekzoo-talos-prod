apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-assistant-certificate
  namespace: apps
spec:
  secretName: home-assistant-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - smarthome.macbytes.io

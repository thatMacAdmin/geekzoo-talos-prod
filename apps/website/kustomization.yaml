apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - certificate.yaml

configMapGenerator:
  - name: website-content
    files:
      - index.html

generatorOptions:
  disableNameSuffixHash: false

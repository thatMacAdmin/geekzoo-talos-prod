apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: inbox-zero-certificate
  namespace: apps
spec:
  secretName: inbox-zero-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - inboxzero.macbytes.io

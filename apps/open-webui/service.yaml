apiVersion: v1
kind: Service
metadata:
  name: open-webui-svc
  namespace: apps
spec:
  selector:
    app: open-webui
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - certificate.yaml

commonLabels:
  app.kubernetes.io/name: open-webui
  app.kubernetes.io/component: chat-application

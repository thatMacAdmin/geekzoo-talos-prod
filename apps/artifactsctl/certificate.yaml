apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: artifactsctl-certificate
  namespace: artifactsctl
spec:
  secretName: artifactsctl-tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - artifactsctl.macbytes.io
